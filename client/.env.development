# ChatConnect — DEV overrides (used by `npm run dev`, never by `vite build`).
# Blank URLs = use the Vite dev proxy → your LOCAL backend on :5000.
# This keeps local development off the deployed/production backend.
VITE_API_URL=
VITE_SOCKET_URL=
VITE_DEMO_MODE=false
